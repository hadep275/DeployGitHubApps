import React from 'react';

const Step1 = () => {
  return (
    <div>
      <h2>Step 1: Create a React App</h2>
      <p>Open your terminal and run:</p>
      <code>npx create vite@latest</code>
    </div>
  );
};

export default Step1;
